"use strict";

var _require = require('crypto'),
    createHash = _require.createHash;

var _require2 = require('../package.json'),
    name = _require2.name; // TODO: increment this version if there are schema changes
// that are not backwards compatible:


var VERSION = '4';
var SHA = 'sha1';
module.exports = {
  SHA: SHA,
  MAGIC_KEY: '_coverageSchema',
  MAGIC_VALUE: createHash(SHA).update(name + '@' + VERSION).digest('hex')
};