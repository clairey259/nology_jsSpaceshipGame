"use strict";

var _require = require('@istanbuljs/schema'),
    defaults = _require.defaults;

var Instrumenter = require('./instrumenter');

var programVisitor = require('./visitor');

var readInitialCoverage = require('./read-coverage');
/**
 * createInstrumenter creates a new instrumenter with the
 * supplied options.
 * @param {Object} opts - instrumenter options. See the documentation
 * for the Instrumenter class.
 */


function createInstrumenter(opts) {
  return new Instrumenter(opts);
}

module.exports = {
  createInstrumenter: createInstrumenter,
  programVisitor: programVisitor,
  readInitialCoverage: readInitialCoverage,
  defaultOpts: defaults.instrumenter
};