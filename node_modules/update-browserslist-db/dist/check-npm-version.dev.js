"use strict";

var _require = require('child_process'),
    execSync = _require.execSync;

var pico = require('picocolors');

try {
  var version = parseInt(execSync('npm -v'));

  if (version <= 6) {
    process.stderr.write(pico.red('Update npm or call ' + pico.yellow('npx browserslist@latest --update-db') + '\n'));
    process.exit(1);
  }
} catch (e) {}