'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
exports["default"] = getConfigsOfProjectsToRun;

var _getProjectDisplayName = _interopRequireDefault(require('./getProjectDisplayName'));

function _interopRequireDefault(obj) {
  return obj && obj.__esModule ? obj : {
    "default": obj
  };
}
/**
 * Copyright (c) Facebook, Inc. and its affiliates. All Rights Reserved.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */


function getConfigsOfProjectsToRun(projectConfigs, opts) {
  var projectFilter = createProjectFilter(opts);
  return projectConfigs.filter(function (config) {
    var name = (0, _getProjectDisplayName["default"])(config);
    return projectFilter(name);
  });
}

function createProjectFilter(opts) {
  var selectProjects = opts.selectProjects,
      ignoreProjects = opts.ignoreProjects;

  var always = function always() {
    return true;
  };

  var selected = selectProjects ? function (name) {
    return name && selectProjects.includes(name);
  } : always;
  var notIgnore = ignoreProjects ? function (name) {
    return !(name && ignoreProjects.includes(name));
  } : always;

  function test(name) {
    return selected(name) && notIgnore(name);
  }

  return test;
}